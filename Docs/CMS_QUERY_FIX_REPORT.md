# CMS_QUERY_FIX_REPORT

## Scope
- src/modules/cms/cms.service.js
- tests/cms/visibility-query.test.js

## Objective Closed
- TD-003 (query correctness risk from duplicate logical conditions)

## Changes Implemented
- Added `buildSectionVisibilityQuery()` to construct section visibility using explicit `$and` composition.
- Preserved required visibility dimensions:
  - page-scoped sections
  - global sections
  - schedule visibility windows
  - city override compatibility
  - null-city fallback
- Added schedule helper with inclusive active-window checks:
  - no schedule window
  - start-only
  - end-only
  - bounded start/end
- Updated `getSectionsByPage(pageId, { cityId })`:
  - cache key now city-aware
  - visibility query now generated by helper
- Updated CMS mutations to trigger safe broad invalidation for page render/fragment surfaces.

## Correctness Matrix Validation
Covered in tests/cms/visibility-query.test.js:
1. page + global matrix
2. scheduled active/inactive windows
3. city override precedence
4. null city fallback
5. mixed visibility clause structure

## Compatibility
- Existing CMS route paths unchanged.
- Existing CMS admin behavior preserved.
- Validator middleware remains intact.
